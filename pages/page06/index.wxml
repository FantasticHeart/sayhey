<view class="wrap page06">
	<view class="list" wx:for="{{focusData}}" wx:key="item">
		<view class="item" data-id="{{item.id}}" bindtap='clickOwn'>
			<image src="{{item.avatar}}" class="face" mode="widthFix"></image>
			<view class="r">
				<view class="nick-name">{{item.name}}</view>
			</view>
		</view>
	</view>
</view>